<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Order Successful | LuxeLook</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background-color: #FFF8F3;
    }
  </style>
</head>

<body>

<!-- Banner -->
<section class="relative w-full mb-6">
  <img src="\wallpaper\pageBanner-1.jpg"
       class="w-full h-[300px] md:h-[350px] object-cover">
  <h1 class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 
             text-white text-3xl md:text-5xl font-bold drop-shadow-xl">
      ORDER SUCCESS
  </h1>
</section>

<!-- Success Card -->
<div class="max-w-2xl mx-auto bg-[#FCF4ED] p-6 md:p-10 rounded-xl shadow-md 
            text-center mb-10">

  <!-- Ordered Product Images -->
  <div class="flex justify-center gap-4 mb-5">
    <% orders.orderedItems.slice(0,2).forEach(item => { %>
      <img src="<%= item.product.productImage[0] %>"
           class="w-20 h-24 md:w-24 md:h-28 rounded-lg object-cover shadow-sm">
    <% }) %>
  </div>

  <h2 class="text-xl md:text-2xl font-semibold text-gray-800 mb-2">
    Thank You for Your Order!
  </h2>

  <p class="text-sm md:text-base text-gray-700 mb-6">
    Your order was placed successfully. We’re getting it ready to ship.
  </p>

  <!-- Buttons -->
  <div class="flex flex-col md:flex-row justify-center gap-3">

    <a href="/orders"
       class="px-6 py-3 rounded-full text-white text-sm md:text-base 
              bg-gradient-to-r from-[#B5654B] to-[#D17C63]
              hover:opacity-90 hover:-translate-y-1 transition-all w-full md:w-auto">
       View Order Details
    </a>

    <a href="/shop"
       class="px-6 py-3 rounded-full text-white text-sm md:text-base 
              bg-gradient-to-r from-[#6B4C3B] to-[#8A6956]
              hover:opacity-90 hover:-translate-y-1 transition-all w-full md:w-auto">
       Continue Shopping
    </a>

  </div>

</div>

</body>
</html>
