<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Order Failed | LuxeLook</title>
  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background-color: #FFF8F3;
    }
  </style>
</head>

<body>

<!-- ⭐ BANNER (RESPONSIVE) -->
<section class="relative w-full mb-6">
  <img src="\wallpaper\pageBanner-1.jpg"
       class="w-full h-[220px] sm:h-[260px] md:h-[330px] lg:h-[380px] xl:h-[420px] object-cover">

  <h1 class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2
             text-white text-3xl sm:text-4xl md:text-5xl font-bold drop-shadow-xl">
      ORDER FAILED
  </h1>
</section>


<!-- ⭐ FAILED CARD (RESPONSIVE) -->
<div class="max-w-3xl mx-auto bg-[#FCF4ED] p-6 sm:p-8 md:p-10 rounded-xl shadow-lg text-center mb-10">

  <h2 class="text-xl sm:text-2xl font-bold text-red-600 mb-2">
    Payment Failed
  </h2>

  <p class="text-sm sm:text-base text-gray-700 mb-5">
    <%= error %>
  </p>

  <!-- Ordered Images -->
  <!-- <div class="flex justify-center flex-wrap gap-4 mb-6">
    <% if (order) { %>
      <% order.orderedItems.forEach(item => { %>
        <img src="<%= item.product.productImage %>"
             class="w-16 h-20 sm:w-20 sm:h-24 rounded-lg object-cover shadow-md" />
      <% }) %>
    <% } %>
  </div> -->

  <!-- Buttons -->
  <div class="flex flex-col sm:flex-row justify-center gap-4">

    <a href="/orders"
       class="px-6 py-3 rounded-full text-white text-sm sm:text-base
              bg-gradient-to-r from-[#6B4C3B] to-[#8A6956]
              hover:opacity-90 hover:-translate-y-1 transition-all w-full sm:w-auto text-center">
       Go to Orders
    </a>

    <a href="/orders/retry-Checkout?orderId=<%= order._id %>"
       class="px-6 py-3 rounded-full text-white text-sm sm:text-base
              bg-gradient-to-r from-[#B5654B] to-[#D17C63]
              hover:opacity-90 hover:-translate-y-1 transition-all w-full sm:w-auto text-center">
       Retry Payment
    </a>

  </div>

</div>

</body>
</html>
